Minimal test setup for this project:

- Runner: Node built-in test runner
- TypeScript support: `--experimental-strip-types`
- Command: `npm test`

Test files should live in `tests/` and use the `*.test.ts` naming pattern.
